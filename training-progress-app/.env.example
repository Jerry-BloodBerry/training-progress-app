# Required environment variables for FitProgress
# Copy this file to environment.ts / environment.prod.ts and fill in real values.
# The publishable key (pk_*) is safe to expose in browser builds.
# NEVER commit your Clerk secret key (sk_*) anywhere in the frontend.

CLERK_PUBLISHABLE_KEY=pk_test_your_key_here
